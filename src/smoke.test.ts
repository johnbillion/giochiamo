import { describe, expect, it } from 'vitest';

// Placeholder so `npm run test` is green before any engine tests exist.
// Delete this once src/engine has real tests.
describe('scaffold', () => {
  it('runs the test suite', () => {
    expect(true).toBe(true);
  });
});
